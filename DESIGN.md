# 基金估值追踪系统 - 开发设计文档

## 一、项目概述

基金估值追踪系统是一个用于管理个人基金投资的 Web 应用，支持添加自选基金、查看实时估值、管理持仓记录、计算收益等功能。

## 二、技术架构

### 2.1 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue 3 | ^3.4 |
| UI 组件库 | Element Plus | ^2.13 |
| 路由 | Vue Router | ^4.6 |
| HTTP 客户端 | Axios | ^1.6 |
| 后端框架 | Express | ^4.18 |
| 数据库 | MySQL | 8.0+ |
| 数据库驱动 | mysql2 | ^3.9 |

### 2.2 项目结构

```
fund-tracker/
├── backend/                    # 后端服务
│   ├── src/
│   │   ├── app.js             # 入口文件
│   │   ├── db/
│   │   │   └── database.js    # 数据库连接与初始化
│   │   ├── routes/
│   │   │   ├── fund.js        # 基金相关接口
│   │   │   └── holding.js     # 持仓相关接口
│   │   └── services/
│   │       └── fundService.js # 基金数据抓取服务
│   ├── .env                   # 环境配置
│   └── package.json
├── frontend/                   # 前端应用
│   ├── src/
│   │   ├── main.js            # 入口文件
│   │   ├── App.vue            # 根组件（布局）
│   │   ├── api/
│   │   │   └── index.js       # API 接口封装
│   │   ├── router/
│   │   │   └── index.js       # 路由配置
│   │   ├── views/
│   │   │   ├── Home.vue       # 首页概览
│   │   │   ├── Funds.vue      # 基金列表
│   │   │   └── Holdings.vue   # 持仓管理
│   │   └── style.css          # 全局样式
│   └── package.json
├── start.bat                   # 一键启动脚本
└── DESIGN.md                   # 本文档
```

## 三、数据库设计

### 3.1 funds 表（自选基金）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| code | VARCHAR(10) | 基金代码，唯一 |
| name | VARCHAR(100) | 基金名称 |
| created_at | TIMESTAMP | 创建时间 |

### 3.2 holdings 表（持仓记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| fund_code | VARCHAR(10) | 基金代码 |
| shares | DECIMAL(16,4) | 持有份额 |
| cost_price | DECIMAL(10,4) | 成本价（买入净值） |
| buy_date | DATE | 买入日期 |
| note | VARCHAR(255) | 备注 |
| created_at | TIMESTAMP | 创建时间 |

## 四、API 接口设计

### 4.1 基金接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/fund/list | 获取自选基金列表（含实时估值和持仓汇总） |
| POST | /api/fund/add | 添加自选基金 |
| DELETE | /api/fund/:code | 删除自选基金（同时删除持仓） |
| GET | /api/fund/estimate/:code | 获取单个基金实时估值 |

### 4.2 持仓接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/holding/:fundCode | 获取某基金的持仓记录 |
| POST | /api/holding | 添加持仓记录 |
| PUT | /api/holding/:id | 修改持仓记录 |
| DELETE | /api/holding/:id | 删除持仓记录 |

## 五、外部数据源

### 5.1 天天基金实时估值接口

- 接口地址：`http://fundgz.1234567.com.cn/js/{基金代码}.js`
- 返回格式：JSONP
- 返回字段：
  - `fundcode` - 基金代码
  - `name` - 基金名称
  - `dwjz` - 单位净值
  - `jzrq` - 净值日期
  - `gsz` - 估算值
  - `gszzl` - 估算涨跌幅(%)
  - `gztime` - 估值时间

## 六、功能模块

### 6.1 首页概览
- 统计卡片：持有基金数、持仓市值、累计收益、收益率
- 今日涨跌分布：上涨/下跌基金列表

### 6.2 基金列表
- 添加/删除自选基金
- 展示实时估值、涨跌幅
- 展示持仓份额、市值、收益、收益率
- 每30秒自动刷新

### 6.3 持仓管理
- 按基金筛选持仓记录
- 添加/编辑/删除持仓
- 基金选择支持模糊搜索

---

## 七、待优化项（TODO）

> 在此处添加你的优化建议，我会根据这里的内容进行开发

### 7.1 功能优化 
7.1.1
- [添加持仓优化为 选择基金 加仓/减仓/转换 操作日期(3点前3点后,要计算买入是否在今天/明天) 金额] 
- [基金列表 批量导入 是否支持截图导入?] 
- [持仓管理 具备同步列表,可一键同步支付宝持仓列表?]
- [今日持仓收益 总持仓收益 统计在持仓管理列表下 10秒刷新一下]
7.1.2
- 基金列表具备拖动排序功能
- 持仓管理添加持仓如果基金列表没有,可快捷添加一个

### 7.2 体验优化

- [ ] 
- [ ] 
- [ ] 

### 7.3 技术优化

- [ ] 
- [ ] 
- [ ] 

---

## 八、启动方式

1. 配置数据库：创建 `fund_tracker` 数据库，编辑 `backend/.env` 填写密码
2. 双击 `start.bat` 一键启动前后端
3. 访问 `http://localhost:5173`
